apiVersion: v1
kind: PersistentVolume
metadata:
  name: db-pv
  namespace: banking-system
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/db
